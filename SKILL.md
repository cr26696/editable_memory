---
name: memory
description: "基于 Joplin 的轻量 AI 长期记忆。通过 CLI（memory.mjs）按需检索与提炼写入，向量判定相关性，四层结构分级管理。禁整库注入，按任务预算召回。"
allowed-tools: Bash
---

# memory — AI 长期记忆

将交互中沉淀的有效经验、客观事实与偏好持久化到 Joplin，供后续任务按需召回。

## 为什么这样设计？

1. **按需检索，拒绝全量塞入上下文**
   全量注入记忆既浪费 Token 又引入噪声。每次任务开始前，只针对当前目标检索高度相关的条目，设置严格的数量（≤5条）与 Token（≤800）上限。
2. **代码做数学判定，模型做语义提炼**
   条目是否重复、是否相关、是否超预算，全部由 `memory.mjs` 基于向量余弦相似度计算，准确且稳定；LLM 仅负责提炼结论和选择所属层级。
3. **四层结构，职责分明**
   不把所有记录混在一起，按信息属性归入不同笔记本，检索时根据层级权重排序。

---

## 记忆四分层（写入时选定）

| 层级 (layer) | 定义与场景 | 权重 | 格式要求 |
|---|---|---|---|
| **fact** | 确定、客观、长期有效的环境或系统事实 | 1.0 | 结论明确，避免主观词 |
| **summary** | 对某个实体/项目的全局概括画像 | 0.9 | 综合提炼，不堆砌细碎步骤 |
| **exp** | 具体一次性踩坑、排错经历或操作教训 | 0.7 | 记录问题核心与最终解法 |
| **belief** | 推断、用户偏好或经验假设（含不确定性） | 0.6 | **正文末尾必须带** `置信度：0.x` |

---

## CLI 命令与对接

脚本位置：`M=<skill-dir>/memory.mjs`，所有命令输出均为 JSON。

### 1. 任务前：检索（recall）
根据任务关键词按需召回，若未命中直接开始做任务，不要反复尝试。

```bash
node $M recall "关键词..."
```
- **内部逻辑**：向量计算余弦相似度 → 过滤低相关（`<0.3`）→ 按「相似度 + 层权重」排序 → 截断至预算内（≤5条 / ≤800 tokens）。
- **返回**：`results: [{ id, layer, title, body, sim }]`。

### 2. 任务后：写入（retain）
任务产生新经验或确认新事实时，提炼 **1~3 条** 写入。严禁直接 dump 对话日志。

```bash
node $M retain <layer> <title> [body] [--conf <0~1>]
```
- **参数说明**：
  - `layer`: `fact` | `belief` | `exp` | `summary`
  - `title`: **一句话核心结论**（标题即索引，优先中文关键词，如 `CentOS 8 缺少 musl 工具链（环境·构建）`）
  - `body`: 补充细节（≤200字）；若为 `belief`，正文需包含 `置信度：0.8` 或通过 `--conf 0.8` 传入
- **内部逻辑**：
  - 相似度 `≥ 0.75`（相同）：自动覆盖合并旧笔记；若为 belief 则置信度累加 +0.1。
  - 相似度 `0.30 ~ 0.75`（相关）：新建笔记；若同实体相关条目累计达 3 条，返回聚合建议。
  - 相似度 `< 0.30`（无关）：直接新建笔记。

### 3. 维护与清理（reflect）
用于检测同实体碎片条目、belief 升级或冲突。

```bash
node $M reflect                 # 步骤 1：扫描并输出建议（JSON）
node $M reflect --apply '<json>' # 步骤 2：应用合并/升级变更
```
- 当某实体下积累多条 `fact`/`exp` 时，reflect 会输出 `aggregate` 建议，由 LLM 综合提炼为 `summary` 后通过 `--apply` 写回。
- 当 `belief` 置信度累积到 `≥ 0.9` 时，建议升级为 `fact`。

### 4. 诊断与排错（doctor / test）

```bash
node $M doctor   # 检查 Joplin 连通性、Notebook/Layer ID 映射、Embedding API
node $M test     # 本地逻辑自检（不依赖外部网络）
```

---

## 规范与限制

1. **标题即检索特征**：不写模糊代词（“这里/本机/上次”），明确写出组件名、环境名与结论。
2. **硬性预算**：单次任务 recall ≤ 2 次；单次注入严格受限，不破坏模型原始上下文窗口。
3. **数据同步**：写入后若需立即被 Joplin 本地全文检索感知，可执行 `joplin sync`（CLI 工具自身基于内存向量库，不受 sync 延迟影响）。
