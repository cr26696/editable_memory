# editable_memory — 系统设计与核心理念

本文档记录 `editable_memory`（AI 长期记忆 CLI）的设计动机、架构权衡与运行机制。

---

## 1. 为什么要做这个项目？

在日常使用中，AI Agent 面临两个根本性的记忆矛盾：
1. **记忆需要持久化**：跨会话、跨任务沉淀的经验（如服务器环境踩坑、用户配置偏好）不能随着会话结束而消失。
2. **上下文空间有限且昂贵**：全量把过去的对话日志或笔记全文塞给大模型，不仅迅速挤爆上下文窗口、大幅增加 API 资费，还会引入大量噪音导致大模型注意力分散。

因此，本系统的核心定位是：**构建一个轻量、跨设备同步、基于确定性程序判定的长期记忆层。**

---

## 2. 三大核心设计原则

### 原则一：按需召回，严格限额
- **不搞全量注入**：只有在任务开始前，才根据当前目标关键词向 Joplin 发起精准检索。
- **硬性预算控制**：单次任务仅限 recall ≤ 2 次；单次注入严格限制在 ≤ 5 条笔记、≤ 800 Token 以内。
- **无匹配就直接开始**：当检索结果为空或相似度不足时，不强行注入泛化信息，直接开始执行任务。

### 原则二：代码做数学判定，模型做语义提炼
- **判定程序化**：条目去重（是否已存在相同条目）、相关性门控（是否达到唤醒门槛）、层级权重排序全由本地 Node CLI（`memory.mjs`）通过向量余弦相似度（Cosine Similarity）进行计算。程序化判定速度快、成本零、结果稳定确定。
- **语义归 LLM**：LLM 仅在任务结束后负责提取「1~3 句高价值事实/结论」，以及判定该结论属于哪一个知识层级。

### 原则三：四层知识结构，权责分明
知识按照稳定性和抽象度分为四层，各自存放在独立的子笔记本中：
1. **fact（客观事实，权重 1.0）**：系统环境、确定配置、客观长期有效的信息。
2. **summary（宏观画像，权重 0.9）**：对某个项目、工具或主机的全景总结与画像。
3. **exp（踩坑经验，权重 0.7）**：一次性的排错教训、具体操作经验（含问题与解法）。
4. **belief（偏好与假设，权重 0.6）**：推断、用户偏好或经验假设，带有不确定性，**正文强制标注置信度（0~1）**。随多次验证可自动累加置信度并升级为 fact。

---

## 3. 架构与同步拓扑

为兼顾跨设备访问（手机/PC 查看与人工编辑）与云端 Agent 常开运行，系统直接基于 **Joplin + WebDAV** 构建：

```
                ┌─────────────────────────────────────┐
                │  常开 Linux 服务器（枢纽节点）           │
                │  · Joplin CLI（无头后台运行）           │
                │     ├─ joplin sync → 自动同步 WebDAV │
                │     └─ REST API (:41184)            │
                │  · Agent CLI (memory.mjs)           │
                └──────────────┬──────────────────────┘
                               │ WebDAV 同步
                               ▼
                  ┌────────────────────────────┐
                  │ 远程 WebDAV 服务（持久化存储）  │
                  └──────────────┬─────────────┘
                    ┌────────────┴────────────┐
                    ▼                         ▼
             手机 Joplin App            PC Joplin 桌面客户端
             （随时查看/人工修改）        （人工编辑/调试）
```

- **极简依赖**：服务器上仅运行单个 Joplin CLI 进程（内存占用仅 ~100MB），不搭建臃肿的多用户服务端或数据库。
- **双向可编辑**：人可以在手机/PC Joplin 上随时修正 AI 记录的错误事实；AI 也可以通过本地 `localhost:41184` 读写更新。

---

## 4. 关键交互流程

### 1. 召回链路（Recall）
```
输入任务关键词 
  → 拉取 Joplin 四层条目 
  → 本地/API 向量化计算余弦相似度 
  → 过滤相似度 < 0.3 的噪音 
  → 按 (相似度 + 笔记本层级权重) 排序 
  → 预算裁剪（≤5条/≤800 tokens）
  → 输出结构化 JSON
```

### 2. 沉淀链路（Retain）
```
任务完成，提取 1~3 条精简结论
  → 调用 retain <layer> <title> <body>
  → 自动与已有笔记比对余弦相似度：
      ├─ 相似度 ≥ 0.75（同类条目）：自动合并正文；若为 belief 则累加置信度 (+0.1)
      ├─ 相似度 0.30 ~ 0.75（相关条目）：创建新条目；若同实体相关条目 ≥ 3 条，提示聚合建议
      └─ 相似度 < 0.30（全新条目）：直接创建
```

### 3. 反思与演进（Reflect）
```
定期或在积累达到阈值时运行 reflect
  → 扫描同实体碎片事实：发现多条零散 fact/exp 时，建议 LLM 提炼合并为单条 summary
  → 扫描高置信度 belief：当置信度累积 ≥ 0.9 时，建议升级为 fact
  → 扫描过期未更新 belief：置信度低且长期未更新时，建议人工审查或降级
```
